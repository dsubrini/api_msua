environment:
  matrix:
  - nodejs_version: "12"
install:
  - yarn
test_script:
  - yarn test
build: off
before_test:
    - SET PGUSER=postgres
    - SET PGPASSWORD=postgres
    - PATH=C:\Program Files\PostgreSQL\13\bin\;%PATH%
    - createdb testdb
services:
    - postgresql134